---
output: flynt-{{ .variant }}.yaml
---
accent: '{{ .amber }}'
background: '{{ .bg }}'
details: {{ if eq .variant "dark" }}darker{{ else }}lighter{{ end }}
foreground: '{{ .tx }}'
terminal_colors:
  bright:
    black: '{{ .bg5 }}'
    blue: '{{ index (index .shades "delft") "300" }}'
    cyan: '{{ index (index .shades "teal") "300" }}'
    green: '{{ index (index .shades "moss") "300" }}'
    magenta: '{{ index (index .shades "plum") "300" }}'
    red: '{{ index (index .shades "ember") "300" }}'
    white: '{{ .tx }}'
    yellow: '{{ index (index .shades "amber") "300" }}'
  normal:
    black: '{{ .bg2 }}'
    blue: '{{ .delft }}'
    cyan: '{{ .teal }}'
    green: '{{ .moss }}'
    magenta: '{{ .plum }}'
    red: '{{ .ember }}'
    white: '{{ .tx2 }}'
    yellow: '{{ .amber }}'
